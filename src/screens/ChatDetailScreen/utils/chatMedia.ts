/** Shared image-picker settings — resize + JPEG quality before upload. */
export const CHAT_IMAGE_PICKER_OPTIONS = {
  mediaType: 'photo' as const,
  quality: 0.3,
  maxWidth: 1280,
  maxHeight: 1280,
};
