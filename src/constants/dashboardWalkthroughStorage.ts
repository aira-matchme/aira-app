/**
 * Legacy AsyncStorage key — no longer used to decide whether to show the tour.
 * Cleared when the user completes the tour (server is source of truth: `isAppTourDone` on profile).
 */
export const DASHBOARD_WALKTHROUGH_STORAGE_KEY = '@aira_dashboard_walkthrough_v1';
